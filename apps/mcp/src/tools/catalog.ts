import type { McpServer } from '@modelcontextprotocol/server';
import type { Ctx } from '../ctx.js';

// implemented by wave 2 — search_cards, get_card, set_progress (SPEC §5 #3–#5).
export function registerCatalogTools(_server: McpServer, _ctx: Ctx): void {
  // no tools yet
}
