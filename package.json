{
  "name": "deckpal",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted, single-user TCG collection tracker \u2014 catalog, prices, deck builder, card scanner, MCP server, and React PWA.",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/cheyras/deckpal.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "migrate": "pnpm --filter @deckpal/db migrate",
    "migrate:status": "pnpm --filter @deckpal/db migrate:status",
    "build": "pnpm --filter @deckpal/db build && pnpm --filter @deckpal/storage build && pnpm --filter deckpal-api build && pnpm --filter deckpal-images build && pnpm --filter deckpal-sync build",
    "typecheck": "pnpm -r typecheck",
    "dev:api": "pnpm --filter deckpal-api dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": []
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "ai": "^7.0.58",
    "sharp": "^0.35.3",
    "tsx": "^4.23.9",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
