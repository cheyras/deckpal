{
  "name": "deckscout",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted, single-user TCG collection tracker — catalog, prices, deck builder, card scanner, MCP server, and React PWA.",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/cheyras/deckscout.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "migrate": "pnpm --filter @deckscout/db migrate",
    "migrate:status": "pnpm --filter @deckscout/db migrate:status",
    "build": "pnpm --filter @deckscout/db build && pnpm --filter @deckscout/storage build && pnpm --filter deckscout-api build && pnpm --filter deckscout-images build && pnpm --filter deckscout-sync build",
    "typecheck": "pnpm -r typecheck",
    "dev:api": "pnpm --filter deckscout-api dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": []
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "ai": "^7.0.58",
    "sharp": "^0.35.3",
    "tsx": "^4.23.9",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
