{
  "name": "deckscout",
  "private": true,
  "version": "0.1.0",
  "description": "Self-hosted, single-user TCG collection tracker — catalog, prices, deck builder, card scanner, MCP server, and React PWA.",
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "https://github.com/cheyras/deckscout.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "migrate": "pnpm --filter @deckscout/db migrate",
    "migrate:status": "pnpm --filter @deckscout/db migrate:status",
    "build": "pnpm --filter @deckscout/db build && pnpm --filter deckscout-api build && pnpm --filter deckscout-images build && pnpm --filter deckscout-sync build",
    "typecheck": "pnpm -r typecheck",
    "dev:api": "pnpm --filter deckscout-api dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": []
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.0"
  }
}
