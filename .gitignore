# ── secrets ───────────────────────────────────────────────────────────────
.env
.env.local
*.secret

# ── node / build ──────────────────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
**/package-lock.json

# ── logs ──────────────────────────────────────────────────────────────────
*.log
**/logs/
*.jsonl

# ── image cache + fetched assets (never committed — DECISIONS.md 2026-07-24) ─
cache/
image-cache/
**/cache/cards/
**/cache/sets/
assets/sprites/
**/*.webp
**/*.avif

# ── transient catalog extract (docker save | tar output) ──────────────────
extract/
*.tar

# ── runtime catalog data dir (the importer reads compiled JSON from here) ───
# Ignore data/* contents but re-include the vendored PokeAPI species CSVs, which ARE
# committed (BSD-3, ~216 KB, static — DEX-DATA.md §B.3). Sprites are NOT here (fetched).
data/*
!data/pokeapi/
# Hand-drawn foil-mask corpus (foil/main track) IS committed — ground-truth
# artifacts, small PNGs, never card art (mask-pipeline SKILL.md).
!data/foil-masks/
# Canon pattern defaults + per-card overrides (foil workbench split) ARE
# committed — tiny JSON, the locked-down recipe truth (routes/foil-lab.ts).
!data/foil-canon/
!data/foil-overrides/
# Adjusted window geometry (foil/mask-refine — pre-flatten state) IS
# committed — tiny JSON sidecars, same rationale as overrides.
!data/foil-windows/

# ── backups + exports (never committed; live outside the repo by default) ──
# Defensive: if a backup/export is ever pointed inside the repo, don't commit it.
deckscout-backups/
deckscout-exports/
pokedex-backups/
pokedex-exports/
*.dump

# ── os cruft ──────────────────────────────────────────────────────────────
.DS_Store

# ── deploy artifacts ─────────────────────────────────────────────────────
# Intentionally untracked, not just deleted — harmless if someone recreates
# deploy/ locally for their own self-host setup.
deploy/

# ── machine-local agent instructions (not portable) ─────────────────────
CLAUDE.local.md
